# wrangler project
.dev.vars*
!.dev.vars.example
.env*
!.env.example
.wrangler/
node_modules/
# Generated by `wrangler types` — safe to regenerate any time.
worker-configuration.d.ts
# Optional local override file: keep the real KV namespace id (and any
# other personal overrides) out of git. Deploy with:
#   npx wrangler deploy -c wrangler.local.jsonc
wrangler.local.jsonc
